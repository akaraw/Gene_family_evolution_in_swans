 #### In the primary sequences directory ####
 for i in *.fa
 do
 base=$(basename $i ".fa")
 perl treefam_scan.pl -cpu 20 -fasta ${i} -dir hmm_lib/ -hmm_file TreeFam9  -e_seq 1e-5 -o ${base}.txt
 grep -v "#" ${base}.txt |sort -k1,1n -k10,10g | awk '!($1 in a){a[$1];print}' > ${base}.fam
 done
 
